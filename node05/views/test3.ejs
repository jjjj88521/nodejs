<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="./css/bootstrap.min.css" />
        <title>user</title>
    </head>
    <body>
        <div class="container">
            <div class="py-3">
                <% if(user){ %>
                <div class="d-flex align-items-end">
                    <img class="rounded-circle" src="<%= user.img %>" />
                    <span class="ms-1 fs-3"><%= user.name %></span>
                </div>
                <div class="py-3">
                    <button class="btn btn-primary btn-logout" id="logout">
                        登出
                    </button>
                </div>
                <% }else { %>
                <button class="btn btn-info btn-login" id="login">登入</button>
                <button class="btn btn-success">註冊</button>
                <% } %>
            </div>
        </div>
        <script src="./js/jquery.min.js"></script>
        <script>
            $(".btn-login").on("click", function () {
                window.location.href = "/login";
            });

            $(".btn-logout").on("click", function () {
                window.location.href = "/logout";
            });
        </script>
    </body>
</html>
